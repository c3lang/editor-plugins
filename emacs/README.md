This adds simple syntax highlighting for C3 in Emacs.
Taken from [Alexey Kutepov's simpc-mode](https://github.com/rexim/simpc-mode)

The current way to install is just by running `load-file` during startup.
